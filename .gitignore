# ── OS files ──
.DS_Store
Thumbs.db
desktop.ini

# ── Editor / IDE ──
.vscode/
.idea/
*.swp
*.swo
*~

# ── Logs ──
*.log
npm-debug.log*

# ── Node (in case of future tooling) ──
node_modules/
package-lock.json
.npm
.yarn-integrity

# ── Env files ──
.env
.env.local
.env.*.local

# ── Build output (future) ──
dist/
build/
.cache/

# ── Backup / temp ──
*.bak
*.tmp
*_backup.html
*_old.html
index_*_patched.html
